
// Update payment data for sample user
const updatePaymentData = () => {
  // const manualUser = Meteor.users.findOne({ username: 'manual' });
  // const aiUser = Meteor.users.findOne({ username: 'ai' });

  // Meteor.users.update({ _id: manualUser._id }, { $set: { stripeCusId: 'cus_BT6T9oax9DcpZN' } });
  // Meteor.users.update({ _id: aiUser._id }, { $set: { stripeCusId: 'cus_BT6ddExtpo5xUH' } });
};


export default updatePaymentData;
